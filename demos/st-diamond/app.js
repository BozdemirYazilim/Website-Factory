const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('on')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(x=>io.observe(x));const c=document.querySelector('.cursor');addEventListener('mousemove',e=>{c.style.left=e.clientX+'px';c.style.top=e.clientY+'px';const x=(e.clientX/innerWidth-.5)*16,y=(e.clientY/innerHeight-.5)*16;document.querySelector('.visual').style.transform=`translate(${x}px,${y}px)`});document.querySelectorAll('a,button,.cards article').forEach(x=>{x.addEventListener('mouseenter',()=>{c.style.width='38px';c.style.height='38px'});x.addEventListener('mouseleave',()=>{c.style.width='10px';c.style.height='10px'})});const menu=document.querySelector('.menu'),nav=document.querySelector('nav');menu.addEventListener('click',()=>{nav.classList.toggle('open');menu.textContent=nav.classList.contains('open')?'KAPAT':'MENU'});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));addEventListener('scroll',()=>{const y=scrollY;document.querySelector('.hero h1').style.transform=`translateY(${y*.08}px)`;document.querySelector('.visual').style.marginTop=`${y*.04}px`},{passive:true});